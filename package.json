{
  "name": "Audica",
  "description": "",
  "version": "0.10.0",
  "homepage": "",
  "engines": {
    "node": ">= 0.8.0"
  },
  "dependencies":{
    "grunt": "0.3.17",
    "grunt-contrib": "0.3.0",
    "colors": "0.6.0-1"
  },
  "scripts": {
    "test": "grunt test-local -v && phantomjs ./test/testRunner.js -v && grunt clean",
    "travis": "grunt travis -v && phantomjs ./test/testRunner.js -v"
  }
}